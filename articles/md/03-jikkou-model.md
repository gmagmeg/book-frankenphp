# 高速＆高パフォーマンスを支える技術

この章では、FrankenPHPの高速＆高パフォーマンスを支えるWorkerモードとOctaneについて説明します。

## Workerモードとは
FrankenPHPには２つのモードが用意されており、１つはシングルプロセス、シングルスレッドのphpとして振る舞うモード。もう一つがこの章でも取り扱う、1プロセス内でマルチスレッド動作するWorkerモードです。Workerモードはマルチスレッドで動作させることで、高いパフォーマンスを実現しています。

### プロセス・スレッドの違い

Workerモードを理解するために、まずプロセスとスレッドの違いを整理します。

 - **プロセス**
OSが独立したメモリ空間を割り当てる実行単位で、プロセス間でメモリは共有されません。メモリが独立しているため、処理間で変数の内容が干渉し合わないという安全性の高さがメリットです。一方でプロセスの起動コストは高いです。

 - **スレッド**
1つのプロセス内で並行して動く実行単位で、同じプロセスのメモリ空間を共有します。スレッドはプロセスに比べて起動コストが低く、データの受け渡しが容易という特性があります。反面、適切な排他制御を行わないと、別のリクエストの処理によって変数が意図せず上書きされる危険性があります。

![シングルプロセス・マルチスレッド](images/single-process-thred.png)

通常のPHPはシングルプロセス・シングルスレッドで動作しますが、マルチスレッド環境に向けて排他制御が施されたスレッドセーフ（TS）版のPHPも存在します。FrankenPHPは内部でこのTS版PHPを使用することで、安全なマルチスレッド実行環境を実現しています。

## Workerモードの仕組み

Workerモードは、前述のTS版PHPを利用してマルチスレッドで動作するFrankenPHPの実行モードです。
このWorkerモードを有効にするには複雑な設定は不要で、起動時に `--workers` オプションでワーカー数を指定するだけです。

```bash
php artisan octane:frankenphp \
  --workers=4
```

しかし、LaravelはスレッドセーフなTS版PHPを前提として設計されていないため、このままWorkerモードで動作させると、予期せぬ不具合やパフォーマンスの悪化を招く可能性があります。
この問題を解決するのが、Laravelの拡張パッケージ、Octaneです。

## Octane
Workerモードでは、FrankenPHPの起動時にWorkerスクリプトと呼ばれるPHPファイルを読み込みます。このWorkerスクリプトはアプリケーションの初期化処理（フレームワークの起動・設定読み込みなど）を一度だけ行い、その後はリクエストを受け付けるループに入ります。各リクエストはループ内で処理され、処理が終わっても初期化済みの状態を保ったまま次のリクエストを待ち受けます。

通常のPHP（CGI/FPMモード）では、リクエストごとにPHPプロセスが起動し、フレームワークや設定ファイルを毎回読み込む必要があります。これに対してWorkerモードでは、初期化コストがアプリケーション起動時の1回のみに抑えられるため、リクエストあたりの処理時間を大幅に短縮できます。

また、複数のWorkerスクリプトが並行して実行されるため、同時に複数のリクエストを処理することも可能です。スレッドが同一プロセスのメモリ空間を共有するという特性を活かし、キャッシュや設定データをWorker間で共有することで、メモリ使用量の削減にも貢献します。
